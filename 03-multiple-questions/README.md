# KEY IDEAS

- Callbacks need to be nested so each question waits for the previous one. This pattern is sometimes called 'callback hell'.
- Example 10 (10-promises-api) shows how async/await makes this much cleaner
